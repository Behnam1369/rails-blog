<%= form_with model: @comment, url: url, method: :post , local: true do |form|%>
  <div>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </div>
  <%= form.submit "Submit" %>
<% end %>